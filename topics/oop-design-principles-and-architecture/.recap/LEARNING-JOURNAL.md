---
created_at: 2026-08-05
---

## 2026-08-05 — Dựng khung topic mới: OOP Design Principles & Architecture
Phác thảo và duyệt roadmap 14 chương (OOP → SOLID → Clean Code → nguyên tắc bổ sung → Design Patterns → Refactoring/Code Smells → Testability → Clean Architecture), chốt ví dụ nghiệp vụ xuyên suốt là hệ thống quản lý đơn hàng của một sàn thương mại điện tử. Tạo `00-README.md` và khung thư mục `examples/`, `exercises/`, `my-work/`. Chưa học chương nào.

Đáng lưu:
- Bản nháp roadmap ban đầu gộp LSP+ISP+DIP vào 1 chương và DRY+KISS+YAGNI+Composition over Inheritance vào 1 chương — bị Instructional Designer chỉ ra vượt tải nhận thức (mỗi nguyên tắc là một bài tập thiết kế riêng, không chỉ là khái niệm). Đã tách: LSP+ISP thành 1 chương, DIP thành chương riêng (vì DIP là nền tảng trực tiếp cho Testability); Composition over Inheritance dời sang mở đầu chương Strategy Pattern để motivate pattern đó, còn DRY+KISS+YAGNI giữ chung vì cùng kể một câu chuyện (loại trùng lặp, không phức tạp hoá, không thêm khả năng mở rộng chưa cần).
- Bản nháp ban đầu dựng một `OrderProcessor` giả định mới để dạy nhận diện code smell — bị loại vì đứt mạch ví dụ xuyên suốt (học viên vừa sửa xong một God Class thì lại gặp một God Class khác không liên quan). Đổi thành: dạy smell mới phát sinh ngay trên chính codebase đã xây, xuất hiện sau khi áp các pattern ở những chương liền trước.
- Chương Testability ban đầu chỉ ở mức Apply (viết test theo khuôn có sẵn) dù gắn nhãn giai đoạn Advanced/Evaluate — nâng cấp thành yêu cầu đánh giá trade-off giữa các cách inject dependency (constructor injection / factory / service locator) để khớp đúng cấp Evaluate.
- Tên topic: cân nhắc `oop-solid-clean-architecture` (loại vì chỉ nêu 3/8 mảng nội dung, bỏ sót Clean Code/Patterns/Refactoring/Testability) và `software-design-foundations` (loại vì quá chung chung, dễ đụng hàng khi có thêm topic khác sau này) trước khi chốt `oop-design-principles-and-architecture`.

## 2026-08-06 — Chương 01: Class, Object, Encapsulation (đã dạy, chưa viết file lesson)
Dạy xong nội dung chương 01 qua hội thoại và vượt chất vấn của Software Architect. Chốt ngôn ngữ code cho cả bundle là TypeScript, chạy bằng Node 24 (type-stripping sẵn có, không cài thêm gì). Dựng thư mục `scratch/chuong-01-encapsulation/` gồm file minh hoạ 5 ca đọc dữ liệu, file bài tập khung, và README. Chưa viết `01-*.md`, người học chưa làm bài tập.

Đáng lưu:
- Phát biểu invariant ban đầu ở dạng thao tác ("thêm item thì phải cập nhật total") — điểm yếu là mỗi thao tác mới lại cần một quy tắc mới, không nói gì về removeItem hay `order.total = 0`. Đã sửa sang dạng điều kiện luôn đúng về dữ liệu: "total luôn bằng tổng unitPrice × qty trên mọi item".
- Lý do chọn getter cho `total` ban đầu có một vế sai: "tách field ra sẽ conflict với `get total`". Đó là chuyện đặt tên (`_total` là hết), không phải đánh đổi thiết kế. Vế đúng còn lại: lưu field thì mọi mutator phải nhớ cập nhật, getter thì không tồn tại trạng thái nào để lệch.
- Software Architect lật ngược lập luận getter: nếu `total` là sự kiện đã xảy ra (đơn đã đặt) chứ không phải dữ liệu suy diễn (giỏ hàng), thì tính lại từ giá sống mới là cái phá invariant. Giải quyết bằng price snapshot — chốt `unitPrice` vào `Item` lúc addItem, giữ nguyên getter.
- Ngộ nhận chính đã sửa: chọn **deep copy** để chặn `getItems().push(...)`. Phản biện chỉ ra deep copy nằm cùng nhóm với chính mối đe doạ người học tự định nghĩa ("sai logic nhưng không có log") — nó nuốt thao tác sai trong im lặng, người gọi tin là mình đã sửa đơn hàng. Đã đổi sang `readonly` hai tầng (field readonly + kiểu trả về `readonly Item[]`): báo đỏ lúc compile, miễn phí lúc chạy.
- Quy tắc rút ra từ 5 ca caller: "mặc định là trả lời câu hỏi, không đưa dữ liệu; chỉ đưa dữ liệu khi câu hỏi không thể biết trước". Nhóm A (hiển thị, cân nặng, tồn kho) → kiểu trả về hẹp riêng cho từng nhu cầu. Nhóm B (khuyến mãi tự lọc) → mới cần `readonly Item[]`. Ca persistence tách riêng, ba hướng đều có giá, để lại chương 14.
- Loại việc viết test cho chương này. Lý do: phòng thủ của thiết kế nằm ở compile-time, `readonly`/`private` bị xoá trước khi test chạy — hai thứ đáng kiểm nhất thì test runtime không với tới. Thay bằng khối 4 dòng vi phạm để comment sẵn trong file, uncomment ra xem editor báo đỏ.
- Loại thư mục `my-work/practice/` cho code nháp trong lúc học, vì trùng chỗ với bài nộp `my-work/NN-{bai-tap}/attempt-N/`. Chốt `scratch/` ở gốc bundle, tách hẳn khỏi cả `exercises/` lẫn `my-work/`.

## 2026-08-06 (phiên 2) — Dựng bài tập 01 và làm rõ ranh giới sự thật/chính sách
Instructional Designer thiết kế bài tập chính thức đầu tiên: `exercises/01-order-tra-loi-hay-dua-du-lieu/` (PROBLEM.md + starter/order.ts), cấp Bloom Apply. Tạo sẵn `my-work/01-order-tra-loi-hay-dua-du-lieu/attempt-1/` với bản chép từ starter. Người học chưa làm bài. Chương 1 vẫn chưa có file `01-*.md`.

Đáng lưu:
- Bài tập đặt nhầm chỗ: khung bài tập ban đầu nằm trong `scratch/`, lẫn với code minh hoạ của buổi dạy. Đã tách đúng quy ước bundle — đề bài ở `exercises/`, bài làm ở `my-work/`, `scratch/` chỉ giữ code minh hoạ. Đã xoá `scratch/chuong-01-encapsulation/order.ts`.
- Loại phương án "chỉ chuyển chỗ order.ts sang my-work/ mà không dựng PROBLEM.md", vì `my-work/` sẽ có thư mục không ứng với đề bài nào trong `exercises/`, phá luôn cách Bước 1 của skill `learn` đối chiếu tiến độ.
- Chốt quy ước cho khoá `skills` trong frontmatter bài tập: kebab-case tiếng Anh, không trộn tiếng Việt. Bản đầu là `["encapsulation", "invariant", "least-privilege", "readonly-hai-tang", "ranh-gioi-su-that-va-chinh-sach"]` — lẫn ba kiểu đặt tên. Lý do phải thống nhất: đây là khoá để đối chiếu giữa các bài tập nên phải khớp chính xác, mà tiếng Việt bỏ dấu không có chuẩn nào để bám. Đổi `readonly-hai-tang` → `immutable-types` (bớt gắn với TypeScript) và `ranh-gioi-su-that-va-chinh-sach` → `responsibility-boundaries` (tái dùng được ở các chương SOLID).
- Hỏi lại Ca 2 (ranh giới `totalWeightGram` vs `calculateShippingFee`) — dấu hiệu lần giảng đầu chỉ nêu kết luận mà chưa cho thấy hậu quả. Lần hai giải thích bằng dòng thời gian 6 tháng: tăng giá cước → thêm nhà vận chuyển thứ hai → campaign "miễn ship khách mới" (khiến `Order` phải biết về khách hàng) → cần so sánh 3 lựa chọn giao hàng (mà getter chỉ trả một số). Chốt câu hỏi phân biệt: không phải "chỗ này có đủ dữ liệu để tính không" mà là "khi công thức này đổi, ai là người yêu cầu đổi" — nếu là người ngoài đội thì logic đó không thuộc class lõi.
- Tên thư mục còn lẫn tiếng Việt/Anh (`01-order-tra-loi-hay-dua-du-lieu`, `scratch/chuong-01-encapsulation` so với topic tiếng Anh) — đã nêu, người học chưa quyết đổi hay giữ.

## 2026-08-10 — Làm và hoàn thành bài tập 01
Viết lại `PROBLEM.md` cho dễ tiếp cận hơn (Technical Writer sửa câu văn, không đổi nội dung đề). Người học tự phân loại 5 nhu cầu (N1–N5), qua nhiều vòng chất vấn (Software Architect) và code review trực tiếp, đến khi bảng phân loại và code trong `attempt-1/order.ts` đều đạt tiêu chí. Bài tập 01 coi như hoàn thành.

Đáng lưu:
- **Phát hiện lỗi thiết kế thật trong đề, không phải hiểu sai của người học**: `PROBLEM.md` bắt buộc "ít nhất 1 nhu cầu ngoài `Order`", nhưng người học chứng minh qua nhiều vòng là cả 5 nhu cầu đều dùng dữ liệu nằm sẵn trên `Item` — không có nhu cầu nào `Order` thực sự không với tới được. Bản sửa đầu (đổi câu hỏi N3 từ "cần tổng thể tích" sang "cần biết đóng thùng cỡ nào") bị người học vặn lại đúng: `Order` vẫn có field `volumeCm3`, đổi câu chữ không đổi việc dữ liệu vẫn phải chảy ra từ `Order`. Instructional Designer xác nhận đây là lỗi constructive alignment (mục tiêu bài chỉ tuyên bố 2 nhóm A/B, "ngoài" chỉ lẻn vào qua Yêu cầu/Tiêu chí, không khớp bộ 8 field đã chọn) và sửa tận gốc: bỏ hẳn nhóm "ngoài", xếp N3 vào nhóm B dùng chung getter với N5, thêm tiêu chí "N3 và N5 phải dùng chung đúng một getter".
- Ngộ nhận đã sửa (N2): người học tự nhận "kiểm tra tồn tại item lạnh là một phép tính" nhưng vẫn giữ nhóm B với lý do "Order không cần tính hộ" — mâu thuẫn với chính quy tắc "có tính toán → A" đang dùng cho các dòng khác. Sau khi bị hỏi lại, tự sửa N2 = A.
- Ngộ nhận đã sửa (N1, lặp 3 lần): ban đầu xếp B/A qua lại với lý do dựa vào `totalPayable` (invariant có sẵn) thay vì đúng thứ N1 cần — tiền thuế và thành tiền **theo từng dòng**, hai giá trị không tồn tại sẵn dưới dạng field, phải tự nhân `unitPrice × qty × vatRate`. Chốt đúng sau khi bị chỉ ra tám field của `Item` không có field nào tên `taxAmount`/`lineTotal`.
- Câu hỏi hay, đáng dùng lại: "tại sao `total` được tách thành method riêng mà tổng thể tích thì không?" — câu trả lời chốt: `total`/`totalPayable` là *invariant* `Order` phải tự giữ đúng cho chính nó (xoá đi thì khái niệm "đơn hàng" mất phần cốt lõi), còn tổng thể tích chỉ là phép tính phục vụ một nhu cầu ngoài, phụ thuộc chính sách hay đổi của bộ phận khác (ngưỡng chọn thùng). Phép thử: *"nó là lời hứa Order giữ cho chính nó, hay chỉ tồn tại để phục vụ một nhu cầu bên ngoài phụ thuộc chính sách hay đổi?"*
- Phương án bị loại: sửa lỗi "một dòng tự kiểm không báo đỏ" bằng cách bọc `Readonly<readonly Item[]>` ở kiểu trả về của getter — không có tác dụng vì đó là bọc thêm lớp thừa lên **mảng**, trong khi lỗ hổng nằm ở **field bên trong từng `Item`** chưa có `readonly`. Sửa đúng chỗ: thêm `readonly` vào từng field của `Item`.
- Bug thật tìm thấy khi tự chạy code (không thấy được nếu chỉ đọc): `priceAfterTax` dùng phép cộng (`unitPrice + qty + (1+vatRate)`) thay vì phép nhân; `{...item, taxAmount, priceAfterTax}` (dùng spread) làm lộ toàn bộ `Item` gốc ở runtime dù kiểu khai báo trông hẹp — TypeScript không báo lỗi vì spread được miễn excess-property-check, phải liệt kê field tường minh mới chặn được; `vatRate` nhập dạng số nguyên (`5`, `10`) thay vì phân số (`0.05`, `0.10`) làm `totalPayable` sai lệch ~10 lần.
- Máy đang chạy Node 22.15.1, không phải Node 24 như đề giả định — `node order.ts` báo `ERR_UNKNOWN_FILE_EXTENSION`. Cách chạy tạm: `node --experimental-strip-types order.ts`.

## 2026-08-10 (phiên 2) — Viết file lesson chương 1 và chuẩn hoá tên trong bundle
Viết `01-oop-foundations.md` — file lesson đầu tiên của bundle, nên nó cũng đặt luôn văn phong và cấu trúc mẫu cho các chương sau. Đổi toàn bộ tên thư mục/file trong bundle sang tiếng Anh (`exercises/01-answer-or-expose-data`, `my-work/01-answer-or-expose-data`, `scratch/01-encapsulation`) bằng `git mv` và cập nhật mọi tham chiếu. Bổ sung roadmap `00-README.md`: những nội dung bị đẩy khỏi chương 1 được ghi vào cột Nội dung chính của chương nhận. Chương 1 coi như hoàn thành; chương tiếp theo là chương 2.

Đáng lưu:
- **Chốt cách tổ chức chương so với bộ bốn thuộc tính OOP.** Người học chỉ ra bản thảo chỉ dạy encapsulation trong khi bộ bốn (abstraction, encapsulation, inheritance, polymorphism) luôn xuất hiện cùng nhau trong code thật, và nêu nguyên tắc: hoặc nói cả bốn, hoặc không nói thuộc tính nào. Phương án bị loại: gộp cả bốn vào chương 1 — lý do loại là quá tải nhận thức (chương 1 lên ~1.400 dòng) và inheritance sẽ bị chạm ở bốn độ sâu khác nhau tại chương 1, 2, 5, 8, dễ trùng lặp và mâu thuẫn giọng. Chốt: thêm mục "Bản đồ bốn thuộc tính" ở đầu chương — một đoạn code `Carrier` có mặt đủ bốn, mô tả gọn từng cái, quan hệ phụ thuộc giữa chúng, bảng thuộc tính → chương đi sâu. Ranh giới giữa chương 1 và 2 được phát biểu tường minh: chương 1 là ranh giới của **một** object, từ chương 2 là quan hệ giữa **nhiều** type.
- **Tiêu đề và filename không được liệt kê một tập con của bộ bốn.** Bị loại hai lần: `Class, Object, Encapsulation` (tiêu đề gốc trong roadmap) và cả `Object, Abstraction, Encapsulation` — lý do loại giống nhau, liệt kê tên vài thuộc tính khiến người đọc hiểu đó là danh sách đủ. Chốt: tiêu đề gọi tên chủ đề (`Nền tảng hướng đối tượng: ranh giới của một object`), filename `01-oop-foundations.md`. Kèm một điều kiện: bốn từ khoá phải giữ ở cột Nội dung chính của roadmap, nếu không sẽ mất khả năng tra "học encapsulation ở chương nào".
- **Nợ nội dung ghi vào roadmap, không ghi vào sổ học tập.** Đề xuất ban đầu của mentor là ghi danh sách nội dung đã đẩy sang chương sau vào sổ; người học sửa lại: sổ là lịch sử phiên, roadmap là bản kế hoạch người khác đọc được, nên nội dung chưa học tới phải nằm ở roadmap. Đã bổ sung vào bảy dòng chương: 3 (Command-Query Separation), 4 (anemic domain model, Law of Demeter, entity vs value object), 5 (Design by Contract), 7 (make illegal states unrepresentable), 9 (named constructor), 13 (leaky abstraction, CQRS, phê phán OOP), 14 (ba hướng persistence và versioning).
- **Bỏ hẳn bộ khung học thuật khỏi thân chương.** Bản thảo giữa có bài báo Parnas 1972, thuật ngữ abstract data type, ví dụ `Stack`, lịch sử Simula/Alan Kay, và cặp thuật ngữ essential/accidental complexity của Brooks. Loại toàn bộ, lý do: với chương Beginner, tên người và mốc năm không thay đổi quyết định nào của người đọc. Giữ lại phần dùng được — tiêu chí "che những quyết định bạn muốn giữ quyền đổi ý" — chuyển vào mục 3 kèm bảng bốn cấp áp dụng hôm nay: object (`private`/`#field`), file (`export`), package (`exports`, `internal/`), service (API và DTO riêng). Tên Parnas còn đúng một dòng ở mục Đọc thêm.
- **Câu hỏi cho thấy chỗ viết quá nén:** "Parnas/ADT là gì vậy". Mục đó nêu tên hai khái niệm và một mốc năm mà không có ví dụ nào, nên người đọc không có gì để bám. Đây là căn cứ trực tiếp dẫn tới quyết định bỏ khung học thuật ở trên.
- **Chốt: mọi ví dụ code trong lesson viết bằng TypeScript.** Bản trước dùng Java cho representation exposure và Tell-Don't-Ask, dùng Go cho "object không cần class". Đã thay hết; ví dụ Go được thay bằng closure TypeScript, vốn mạnh hơn ở một điểm là biến trong closure riêng tư thật lúc chạy, không phải riêng tư lúc biên dịch. Ngôn ngữ khác chỉ còn xuất hiện khi nội dung đang nói về khác biệt giữa các ngôn ngữ (bảng `private` bảo đảm được gì).
- **Bản 936 dòng bị loại vì sai cấp độ.** Chương 1 gắn nhãn Beginner/Understand trong roadmap nhưng được viết ở mức tài liệu tham chiếu. Đã cắt các phần nâng cao sang chương sau và giữ lại phần bài tập 01 đang đo. Bổ sung theo yêu cầu người học: mục so sánh cùng một bài toán viết theo ba cách (procedural / OOP / functional), kèm ba điều kiện để OOP là lựa chọn đúng — có state đổi theo thời gian, có quy tắc phải luôn đúng, và có nhiều chỗ cùng tác động lên state đó.
